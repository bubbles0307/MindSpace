<!DOCTYPE html>
<html>
<head>
  <title>Edit Journal Entry</title>
  <link rel="stylesheet" href="/Entries.css">
  <style>
    .container {
      max-width: 500px;
      margin: 20px auto;
      text-align: center;
    }
    form {
      display: flex;
      flex-direction: column;
      gap: 10px;
    }
  </style>
</head>
<body>
  <div class="container">
  <h1>Edit Your Journal Entry</h1>
  <form action="/journal/edit/<%= entry._id %>" method="POST">
    <label>Date:</label>
    <input type="date" name="date" value="<%= entry.date.toISOString().split('T')[0] %>" required>
    <label>Title:</label>
    <input type="text" name="title" value="<%= entry.title %>" required>
    <label>Content:</label>
    <textarea name="content" required><%= entry.content %></textarea>
    <button type="submit">Update</button>
  </form>
  <a href="/journal/view-entries">Cancel</a>
</div>
</body>
</html>
